language: node_js

node_js:
  - 8.16.0

cache: npm

install:
  - npm ci

script:
  - npm test
  - npm run build

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $github_token
  on:
    branch: master
